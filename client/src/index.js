import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App';

ReactDOM.render(<App />, document.getElementById('root'));


//Since the starting point for the application is the index.js file in src folder
//So we simply import another file from our components folder, 
//which you will create, and add another file in the folder called App.js